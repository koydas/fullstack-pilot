apiVersion: apps/v1
kind: Deployment
metadata:
  name: fullstack-pilot-dependencies-service
  namespace: fullstack-pilot
  labels:
    app: fullstack-pilot-dependencies-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fullstack-pilot-dependencies-service
  template:
    metadata:
      labels:
        app: fullstack-pilot-dependencies-service
    spec:
      containers:
        - name: dependencies-service
          image: ghcr.io/koydas/fullstack-pilot/dependencies-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4003
          envFrom:
            - configMapRef:
                name: dependencies-service-config
            - secretRef:
                name: dependencies-service-secrets
          readinessProbe:
            tcpSocket:
              port: 4003
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 4003
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 512Mi
